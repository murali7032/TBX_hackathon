# TBX Finance Frontend

React + Vite chat UI adapted from [finance-assistant](https://github.com/murali7032/finance-assistant.git).

```powershell
npm install
npm run dev
```

Set `VITE_API_URL` (default `http://127.0.0.1:8000`). Backend must be running with `CURSOR_API_KEY` set.
